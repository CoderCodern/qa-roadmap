---
id: 2
title: "Test types"
phase: 1
estMinutes: 60
tags: ["fundamentals", "types"]
---

## Intro

<En>
Not all tests are created equal. Different test types serve different purposes, and knowing which to use — and when — is one of the core skills of a QA engineer.
</En>
<Vi>
Không phải tất cả các test đều như nhau. Các loại test khác nhau phục vụ các mục đích khác nhau, và biết cách sử dụng loại nào — và khi nào — là một trong những kỹ năng cốt lõi của kỹ sư QA.
</Vi>

## Concept

<En>
**Functional testing** verifies that a feature does what the requirements say it should. Example: the login button submits credentials and navigates to the dashboard.

**Regression testing** ensures that existing features still work after new code is added. Every time a developer ships a change, regression tests catch unintended side effects.

**Smoke testing** is a quick sanity check run immediately after a new build. It checks the most critical paths (can the app start? can a user log in?) to decide whether deeper testing is worthwhile. If smoke tests fail, stop — the build is broken.

**Sanity testing** is a narrow, focused check performed after a specific bug fix or minor change to confirm the fix works and nothing obvious broke around it.

**Exploratory testing** is unscripted investigation. The tester uses intuition, experience, and curiosity to discover unexpected defects that scripted tests would miss. It is especially valuable in areas of high risk or low test coverage.
</En>
<Vi>
**Kiểm thử chức năng** xác minh rằng một tính năng làm đúng những gì yêu cầu đặt ra. Ví dụ: nút đăng nhập gửi thông tin xác thực và điều hướng đến dashboard.

**Kiểm thử hồi quy** đảm bảo rằng các tính năng hiện có vẫn hoạt động sau khi thêm code mới. Mỗi lần developer giao một thay đổi, các test hồi quy sẽ phát hiện các tác dụng phụ không mong muốn.

**Kiểm thử khói** là kiểm tra tỉnh táo nhanh chóng được chạy ngay sau một bản build mới. Nó kiểm tra các đường dẫn quan trọng nhất (ứng dụng có thể khởi động không? Người dùng có thể đăng nhập không?) để quyết định xem kiểm thử sâu hơn có đáng không. Nếu test khói thất bại, hãy dừng lại — bản build bị hỏng.

**Kiểm thử tỉnh táo** là kiểm tra hẹp, tập trung được thực hiện sau khi sửa một bug cụ thể hoặc thay đổi nhỏ để xác nhận rằng bản sửa hoạt động và không có gì hiển nhiên bị vỡ xung quanh đó.

**Kiểm thử khám phá** là điều tra không có kịch bản. Tester sử dụng trực giác, kinh nghiệm và sự tò mò để phát hiện các lỗi bất ngờ mà các test có kịch bản sẽ bỏ lỡ. Điều này đặc biệt có giá trị trong các lĩnh vực có rủi ro cao hoặc độ bao phủ test thấp.
</Vi>

## Code / Demo

```python
# Pseudo-code illustrating test type usage in a CI pipeline
# (This is not real pytest code — just a conceptual illustration)

# Smoke tests run first — fast, critical path only
def test_smoke_app_starts():
    response = get("/health")
    assert response.status == 200

def test_smoke_login():
    result = login("admin@example.com", "password123")
    assert result.success is True

# Functional tests run if smoke passes
def test_login_redirects_to_dashboard():
    result = login("user@example.com", "secret")
    assert result.redirect_url == "/dashboard"

# Regression suite runs nightly on all features
def test_regression_password_reset_still_works():
    ...
```

## Exercise

<ExerciseBox>
<En>
- [ ] Pick a website you use daily and name one functional test, one smoke test, and one regression test for it
- [ ] Explain in your own words why smoke tests are run before the full regression suite
- [ ] Find an area of a real app where exploratory testing would add more value than scripted tests
</En>
<Vi>
- [ ] Chọn một website bạn dùng hàng ngày và đặt tên cho một functional test, một smoke test và một regression test
- [ ] Giải thích bằng lời của bạn tại sao smoke test được chạy trước bộ regression đầy đủ
- [ ] Tìm một khu vực trong ứng dụng thực tế mà kiểm thử khám phá mang lại nhiều giá trị hơn test có kịch bản
</Vi>
</ExerciseBox>

## Quiz

<Quiz questions={[
  {
    q: "You just deployed a hotfix. Which test type do you run FIRST?",
    qVi: "Bạn vừa triển khai một hotfix. Loại kiểm thử nào bạn chạy TRƯỚC TIÊN?",
    choices: [
      "Full regression suite",
      "Exploratory testing",
      "Smoke test",
      "Load test"
    ],
    choicesVi: [
      "Bộ regression đầy đủ",
      "Kiểm thử khám phá",
      "Smoke test",
      "Kiểm thử tải"
    ],
    answer: 2,
    explain: "A smoke test quickly validates that the core of the application is still functioning after the deployment, before you invest time in deeper testing.",
    explainVi: "Smoke test nhanh chóng xác nhận phần cốt lõi của ứng dụng vẫn hoạt động sau khi triển khai, trước khi bạn đầu tư thời gian cho kiểm thử sâu hơn."
  },
  {
    q: "Which test type is deliberately unscripted and relies on tester intuition?",
    qVi: "Loại kiểm thử nào cố ý không có kịch bản và dựa vào trực giác của tester?",
    choices: [
      "Regression testing",
      "Sanity testing",
      "Functional testing",
      "Exploratory testing"
    ],
    choicesVi: [
      "Kiểm thử hồi quy",
      "Kiểm thử tỉnh táo",
      "Kiểm thử chức năng",
      "Kiểm thử khám phá"
    ],
    answer: 3,
    explain: "Exploratory testing is unscripted — the tester investigates the application freely, using experience and curiosity to find unexpected defects.",
    explainVi: "Kiểm thử khám phá không có kịch bản — tester điều tra ứng dụng tự do, dùng kinh nghiệm và sự tò mò để tìm ra lỗi bất ngờ."
  },
  {
    q: "Which test type verifies that different modules or components work correctly together?",
    qVi: "Loại kiểm thử nào xác minh rằng các module hoặc thành phần khác nhau hoạt động đúng cùng nhau?",
    choices: [
      "Unit testing",
      "Integration testing",
      "E2E testing",
      "Smoke testing"
    ],
    choicesVi: [
      "Unit testing",
      "Integration testing",
      "E2E testing",
      "Smoke testing"
    ],
    answer: 1,
    explain: "Integration testing focuses on the interfaces and interactions between components. It sits between unit tests (single units in isolation) and E2E tests (full user flows).",
    explainVi: "Integration testing tập trung vào các giao diện và tương tác giữa các thành phần. Nó nằm giữa unit test (đơn vị độc lập) và E2E test (luồng người dùng đầy đủ)."
  },
  {
    q: "What is the difference between re-testing and regression testing?",
    qVi: "Sự khác biệt giữa re-testing và regression testing là gì?",
    choices: [
      "They are the same thing with different names",
      "Re-testing verifies a specific fixed bug; regression testing checks nothing else was broken by the fix",
      "Regression testing is automated; re-testing is always manual",
      "Re-testing is done by developers; regression by QA"
    ],
    choicesVi: [
      "Chúng giống nhau, chỉ khác tên",
      "Re-testing xác minh một bug đã sửa cụ thể; regression testing kiểm tra không có gì khác bị phá vỡ bởi bản sửa đó",
      "Regression testing là tự động; re-testing luôn là thủ công",
      "Re-testing do developer thực hiện; regression do QA"
    ],
    answer: 1,
    explain: "Re-testing confirms the specific bug is fixed. Regression testing then checks the broader system to ensure the fix didn't inadvertently break previously working features.",
    explainVi: "Re-testing xác nhận bug cụ thể đã được sửa. Regression testing sau đó kiểm tra hệ thống rộng hơn để đảm bảo bản sửa không vô tình phá vỡ các tính năng đang hoạt động trước đó."
  },
  {
    q: "Which test type simulates expected production load to check system stability?",
    qVi: "Loại kiểm thử nào mô phỏng tải sản xuất dự kiến để kiểm tra sự ổn định của hệ thống?",
    choices: [
      "Smoke testing",
      "Regression testing",
      "Load testing",
      "Unit testing"
    ],
    choicesVi: [
      "Smoke testing",
      "Regression testing",
      "Load testing",
      "Unit testing"
    ],
    answer: 2,
    explain: "Load testing verifies the system behaves correctly under expected concurrent user loads. It's a subset of performance testing — distinct from stress testing which pushes beyond normal limits.",
    explainVi: "Load testing xác minh hệ thống hoạt động đúng dưới tải người dùng đồng thời dự kiến. Đây là tập con của performance testing — khác với stress testing vượt quá giới hạn bình thường."
  },
  {
    q: "Which test type validates the full user journey from start to finish across all system layers?",
    qVi: "Loại kiểm thử nào xác nhận toàn bộ hành trình người dùng từ đầu đến cuối trên tất cả các tầng hệ thống?",
    choices: [
      "Unit testing",
      "Integration testing",
      "End-to-End (E2E) testing",
      "Static testing"
    ],
    choicesVi: [
      "Unit testing",
      "Integration testing",
      "Kiểm thử End-to-End (E2E)",
      "Kiểm thử tĩnh"
    ],
    answer: 2,
    explain: "E2E testing exercises the entire application stack — UI, APIs, databases — simulating real user scenarios. It provides the highest confidence but is also the slowest and most expensive to maintain.",
    explainVi: "E2E testing kiểm tra toàn bộ ngăn xếp ứng dụng — UI, API, cơ sở dữ liệu — mô phỏng các kịch bản người dùng thực. Nó cho độ tin cậy cao nhất nhưng cũng chậm nhất và tốn kém nhất để bảo trì."
  }
]} />

## Resources

<ResourceList resources={[
  { title: "ISTQB — Test Types", url: "https://www.istqb.org/" },
  { title: "Ministry of Testing — Test Types Guide", url: "https://www.ministryoftesting.com/" },
  { title: "Test Automation University", url: "https://testautomationu.applitools.com/" }
]} />
