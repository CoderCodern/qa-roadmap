---
id: 9
title: "Variables & data types"
phase: 2
estMinutes: 60
tags: ["python", "basics"]
---

## Intro

<En>
Every Python program works with data. Variables are named containers that hold data, and every piece of data has a type. Understanding Python's built-in types and how to inspect them is the foundation for writing any kind of test logic.
</En>
<Vi>
Mọi chương trình Python đều làm việc với dữ liệu. Biến là các container có tên chứa dữ liệu, và mọi dữ liệu đều có một kiểu. Hiểu các kiểu dữ liệu tích hợp của Python và cách kiểm tra chúng là nền tảng để viết bất kỳ loại logic test nào.
</Vi>

## Concept

<En>
**Core data types you'll use constantly in testing:**

| Type | Example | Common use in tests |
|------|---------|---------------------|
| `int` | `42` | status codes, counts |
| `float` | `3.14` | prices, percentages |
| `str` | `"hello"` | usernames, messages, URLs |
| `bool` | `True / False` | assertion results, feature flags |
| `None` | `None` | absent values, empty responses |

**Key rules:**
- Python is dynamically typed — you don't declare the type; Python infers it.
- Use `type(x)` to check the type of any variable.
- Use `isinstance(x, SomeType)` in tests — it handles inheritance correctly.
- `None` is not the same as `0`, `False`, or `""`. Be explicit when checking for `None`.

**Type coercion:** Python won't silently convert between types. `"5" + 5` raises a `TypeError`. Convert explicitly: `int("5") + 5 == 10`.
</En>
<Vi>
**Các kiểu dữ liệu cốt lõi bạn sẽ dùng liên tục trong kiểm thử:**

| Kiểu | Ví dụ | Dùng phổ biến trong test |
|------|---------|---------------------|
| `int` | `42` | mã trạng thái, số đếm |
| `float` | `3.14` | giá cả, phần trăm |
| `str` | `"hello"` | tên người dùng, thông báo, URL |
| `bool` | `True / False` | kết quả assertion, feature flags |
| `None` | `None` | giá trị vắng mặt, response rỗng |

**Các quy tắc quan trọng:**
- Python là ngôn ngữ kiểu động — bạn không khai báo kiểu; Python suy luận nó.
- Dùng `type(x)` để kiểm tra kiểu của bất kỳ biến nào.
- Dùng `isinstance(x, SomeType)` trong test — nó xử lý kế thừa đúng cách.
- `None` không giống `0`, `False`, hay `""`. Hãy rõ ràng khi kiểm tra `None`.

**Chuyển đổi kiểu:** Python sẽ không tự động chuyển đổi giữa các kiểu. `"5" + 5` sẽ gây ra `TypeError`. Chuyển đổi tường minh: `int("5") + 5 == 10`.
</Vi>

## Code / Demo

<CodePlayground code={`# Variables — no type declarations needed
status_code = 200
username = "alice"
is_logged_in = True
response_body = None
price = 29.99

# Check types
print(type(status_code))    # <class 'int'>
print(type(username))       # <class 'str'>
print(type(is_logged_in))   # <class 'bool'>
print(type(response_body))  # <class 'NoneType'>

# isinstance is safer in tests than type() ==
assert isinstance(status_code, int)
assert isinstance(username, str)

# Truthiness — common in test conditions
assert status_code          # 200 is truthy
assert not response_body    # None is falsy
assert is_logged_in         # True is truthy

# Explicit None check
if response_body is None:
    print("No response body — expected for 204 responses")

# Type conversion
raw = "404"
code = int(raw)
assert code == 404
print(f"Converted '{raw}' to integer {code}")
`} />

## Exercise

<CodeExerciseGroup pointsPerExercise={10} exercises={[
  {
    title: "Exercise 1 — Create typed variables",
    code: `# Represent a hypothetical API response with typed variables
# Replace each ??? with an appropriate value

# An HTTP status code (int)
status_code = ???

# A response message (str)
message = ???

# Did the request succeed? (bool)
success = ???

# Response body not yet loaded (None)
data_payload = ???

# --- Verify: print types ---
print(type(status_code))    # should be <class 'int'>
print(type(message))        # should be <class 'str'>
print(type(success))        # should be <class 'bool'>
print(type(data_payload))   # should be <class 'NoneType'>`
  },
  {
    title: "Exercise 2 — Inspect types with isinstance()",
    code: `# Use type() and isinstance() to inspect variables
status_code = 200
message = "Login successful"
success = True
data_payload = None

# TODO: Print the type of each variable using type()


# TODO: Assert each variable has the correct type with isinstance()
# e.g.  assert isinstance(status_code, int)


print("All type assertions passed!")`
  },
  {
    title: "Exercise 3 — Write passing and failing assertions",
    code: `# Write assertions and observe what happens when they fail
status_code = 200

# TODO: Write an assertion that PASSES (should not raise an error)
assert ???

# TODO: Write an assertion that intentionally FAILS
# Run it to see the AssertionError, then comment it out to continue
assert ???, "Custom failure message here"`
  }
]}>
<En>
Complete the three exercises below. Each editor has comments guiding you — replace the `???` with real Python code, then click **Run** to verify. When done, submit all for AI review.
</En>
<Vi>
Hoàn thành ba bài tập bên dưới. Mỗi editor có comment hướng dẫn — thay `???` bằng code Python thực tế, sau đó nhấn **Run** để kiểm tra. Khi xong, nộp tất cả để AI kiểm tra.
</Vi>
</CodeExerciseGroup>

## Quiz

<Quiz questions={[
  {
    q: "What does `isinstance(x, str)` return if x = 'hello'?",
    qVi: "`isinstance(x, str)` trả về gì nếu x = 'hello'?",
    choices: [
      "False",
      "True",
      "TypeError",
      "'str'"
    ],
    choicesVi: [
      "False",
      "True",
      "TypeError",
      "'str'"
    ],
    answer: 1,
    explain: "isinstance checks whether a value is an instance of a given type. 'hello' is a str, so it returns True. isinstance is preferred over type(x) == str because it handles subclasses correctly.",
    explainVi: "isinstance kiểm tra xem một giá trị có phải là instance của kiểu đã cho không. 'hello' là str, nên trả về True. isinstance được ưu tiên hơn type(x) == str vì nó xử lý lớp con đúng cách."
  },
  {
    q: "Which of these values is falsy in Python?",
    qVi: "Giá trị nào trong số này là falsy trong Python?",
    choices: [
      "1",
      "'False'",
      "None",
      "0.1"
    ],
    choicesVi: [
      "1",
      "'False'",
      "None",
      "0.1"
    ],
    answer: 2,
    explain: "None is falsy in Python, as are 0, 0.0, empty strings '', empty lists [], and empty dicts {}. The string 'False' is truthy because it's a non-empty string.",
    explainVi: "None là falsy trong Python, cũng như 0, 0.0, chuỗi rỗng '', danh sách rỗng [] và dict rỗng {}. Chuỗi 'False' là truthy vì đó là chuỗi không rỗng."
  },
  {
    q: "What does `int('404')` return?",
    qVi: "`int('404')` trả về gì?",
    choices: [
      "'404' (unchanged string)",
      "404 (integer)",
      "404.0 (float)",
      "TypeError"
    ],
    choicesVi: [
      "'404' (chuỗi không đổi)",
      "404 (số nguyên)",
      "404.0 (số thực)",
      "TypeError"
    ],
    answer: 1,
    explain: "int() converts a string to an integer when the string contains a valid integer literal. This is explicit type conversion — Python never does it automatically.",
    explainVi: "int() chuyển đổi chuỗi thành số nguyên khi chuỗi chứa ký tự số nguyên hợp lệ. Đây là chuyển đổi kiểu tường minh — Python không bao giờ tự động làm điều này."
  },
  {
    q: "Which is the correct way to check if a variable is None?",
    qVi: "Cách nào đúng để kiểm tra xem một biến có phải là None không?",
    choices: [
      "if value == None:",
      "if value is None:",
      "if value === None:",
      "if not value:"
    ],
    choicesVi: [
      "if value == None:",
      "if value is None:",
      "if value === None:",
      "if not value:"
    ],
    answer: 1,
    explain: "Use 'is None' for identity checks against None. '== None' works but is considered bad style (PEP 8). '===' doesn't exist in Python. 'not value' is too broad — it's also True for 0, False, and empty strings.",
    explainVi: "Dùng 'is None' để kiểm tra đồng nhất với None. '== None' hoạt động nhưng bị coi là xấu (PEP 8). '===' không tồn tại trong Python. 'not value' quá rộng — nó cũng True với 0, False và chuỗi rỗng."
  },
  {
    q: "What happens when you run `'5' + 5` in Python?",
    qVi: "Điều gì xảy ra khi chạy `'5' + 5` trong Python?",
    choices: [
      "Returns 10",
      "Returns '55'",
      "Raises TypeError",
      "Returns '5 + 5'"
    ],
    choicesVi: [
      "Trả về 10",
      "Trả về '55'",
      "Ném TypeError",
      "Trả về '5 + 5'"
    ],
    answer: 2,
    explain: "Python doesn't silently coerce types. Adding a str and an int raises TypeError: can only concatenate str (not 'int') to str. You must convert explicitly: int('5') + 5 or '5' + str(5).",
    explainVi: "Python không tự động ép kiểu. Cộng str và int ném TypeError: chỉ có thể nối str (không phải 'int') vào str. Bạn phải chuyển đổi tường minh: int('5') + 5 hoặc '5' + str(5)."
  },
  {
    q: "Which function returns the type of a variable as a string you can print?",
    qVi: "Hàm nào trả về kiểu của biến dưới dạng chuỗi có thể in ra?",
    choices: [
      "typeof(x)",
      "x.type()",
      "type(x)",
      "gettype(x)"
    ],
    choicesVi: [
      "typeof(x)",
      "x.type()",
      "type(x)",
      "gettype(x)"
    ],
    answer: 2,
    explain: "type(x) is the built-in function that returns the type object, e.g. <class 'int'>. typeof is JavaScript, x.type() and gettype() don't exist in Python.",
    explainVi: "type(x) là hàm tích hợp trả về đối tượng kiểu, ví dụ <class 'int'>. typeof là JavaScript, x.type() và gettype() không tồn tại trong Python."
  },
  {
    q: "What does `type(True)` return in Python?",
    qVi: "`type(True)` trả về gì trong Python?",
    choices: [
      "<class 'int'>",
      "<class 'boolean'>",
      "<class 'bool'>",
      "bool"
    ],
    choicesVi: [
      "<class 'int'>",
      "<class 'boolean'>",
      "<class 'bool'>",
      "bool"
    ],
    answer: 2,
    explain: "type(True) returns <class 'bool'>. Although bool is a subclass of int in Python (True == 1), its type is 'bool' not 'int'. There is no 'boolean' type — Python uses 'bool'.",
    explainVi: "type(True) trả về <class 'bool'>. Mặc dù bool là lớp con của int trong Python (True == 1), kiểu của nó là 'bool' không phải 'int'. Không có kiểu 'boolean' — Python dùng 'bool'."
  },
  {
    q: "What is the result of `bool('')`?",
    qVi: "Kết quả của `bool('')` là gì?",
    choices: [
      "True",
      "False",
      "TypeError",
      "None"
    ],
    choicesVi: [
      "True",
      "False",
      "TypeError",
      "None"
    ],
    answer: 1,
    explain: "An empty string is falsy in Python, so bool('') returns False. Non-empty strings like 'a' or even '0' are truthy. This is why 'if not some_string:' is a common empty-string guard.",
    explainVi: "Chuỗi rỗng là falsy trong Python, nên bool('') trả về False. Các chuỗi không rỗng như 'a' hay thậm chí '0' là truthy. Đây là lý do 'if not some_string:' là cách kiểm tra chuỗi rỗng phổ biến."
  },
  {
    q: "Which variable naming style is idiomatic Python (PEP 8)?",
    qVi: "Phong cách đặt tên biến nào phù hợp với Python (PEP 8)?",
    choices: [
      "userName",
      "user-name",
      "UserName",
      "user_name"
    ],
    choicesVi: [
      "userName",
      "user-name",
      "UserName",
      "user_name"
    ],
    answer: 3,
    explain: "PEP 8 (Python's official style guide) requires snake_case for variable and function names. CamelCase is reserved for class names. Hyphens are invalid in Python identifiers and will cause a SyntaxError.",
    explainVi: "PEP 8 (hướng dẫn phong cách Python chính thức) yêu cầu snake_case cho biến và hàm. CamelCase dành cho tên class. Dấu gạch ngang không hợp lệ trong định danh Python và sẽ gây SyntaxError."
  },
  {
    q: "What does `str(42)` return?",
    qVi: "`str(42)` trả về gì?",
    choices: [
      "42 (int unchanged)",
      "'42' (string)",
      "TypeError",
      "42.0 (float)"
    ],
    choicesVi: [
      "42 (int không đổi)",
      "'42' (chuỗi)",
      "TypeError",
      "42.0 (số thực)"
    ],
    answer: 1,
    explain: "str() converts any value to its string representation. str(42) returns the string '42'. This is the explicit way to convert numbers to strings — Python will not do it automatically.",
    explainVi: "str() chuyển đổi bất kỳ giá trị nào sang biểu diễn chuỗi. str(42) trả về chuỗi '42'. Đây là cách tường minh để chuyển số thành chuỗi — Python không tự động làm điều này."
  }
]} />

## Resources

<ResourceList resources={[
  { title: "Python Built-in Types — docs.python.org", url: "https://docs.python.org/3/library/stdtypes.html" },
  { title: "Real Python — Variables", url: "https://realpython.com/" }
]} />
