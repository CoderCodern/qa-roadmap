---
id: 8
title: "Install Python & set up VS Code"
phase: 2
estMinutes: 60
tags: ["python", "setup"]
---

## Intro

<En>
Python is the most widely used language for QA automation. Before writing a single test, you need a working environment. Today you will install Python, set up VS Code, and run your first Python program.
</En>
<Vi>
Python là ngôn ngữ được sử dụng rộng rãi nhất cho tự động hoá QA. Trước khi viết một test nào, bạn cần một môi trường hoạt động được. Hôm nay bạn sẽ cài đặt Python, thiết lập VS Code và chạy chương trình Python đầu tiên của mình.
</Vi>

## Concept

<En>
**Why Python for QA?**
Python is readable, cross-platform, has a massive ecosystem of testing libraries (pytest, Playwright, requests), and is the default language for most automation frameworks. If you can read Python, you can read most test codebases.

**What you need:**
- **Python 3.11+** — the runtime that executes your code
- **pip** — Python's package installer (bundled with Python)
- **VS Code** — a lightweight editor with strong Python support
- **Python extension** for VS Code (by Microsoft)

**Virtual environments:** Python projects use isolated environments (`venv`) so that packages installed for one project do not affect another. Think of it as a sandbox — create one per project.

**Terminal basics you need:**
- `python --version` — check version
- `python script.py` — run a file
- `pip install package` — install a library
- `python -m venv .venv` — create a virtual environment
- `source .venv/bin/activate` (macOS/Linux) or `.venv\Scripts\activate` (Windows) — activate it
</En>
<Vi>
**Tại sao dùng Python cho QA?**
Python dễ đọc, đa nền tảng, có hệ sinh thái thư viện kiểm thử khổng lồ (pytest, Playwright, requests) và là ngôn ngữ mặc định cho hầu hết các framework tự động hoá. Nếu bạn đọc được Python, bạn có thể đọc được hầu hết các codebase test.

**Những gì bạn cần:**
- **Python 3.11+** — runtime thực thi code của bạn
- **pip** — trình cài đặt gói của Python (đi kèm với Python)
- **VS Code** — editor nhẹ với hỗ trợ Python mạnh mẽ
- **Python extension** cho VS Code (của Microsoft)

**Môi trường ảo:** Các dự án Python dùng môi trường độc lập (`venv`) để các gói cài đặt cho dự án này không ảnh hưởng đến dự án khác. Hãy nghĩ nó như một hộp cát — tạo một cái cho mỗi dự án.

**Các lệnh terminal cơ bản bạn cần:**
- `python --version` — kiểm tra phiên bản
- `python script.py` — chạy một file
- `pip install package` — cài đặt thư viện
- `python -m venv .venv` — tạo môi trường ảo
- `source .venv/bin/activate` (macOS/Linux) hoặc `.venv\Scripts\activate` (Windows) — kích hoạt nó
</Vi>

## Code / Demo

```python
# Verify your setup — run this after installing Python
import sys
import platform

print(f"Python version: {sys.version}")
print(f"Platform: {platform.system()} {platform.release()}")

# Your first real Python line — a simple assertion
expected = "hello"
actual = "hello"
assert actual == expected, f"Expected '{expected}' but got '{actual}'"
print("First assertion passed!")
```

## Exercise

<InstallGuide steps={[
  {
    title: "Install Python 3.11+",
    titleVi: "Cài Python 3.11+",
    substeps: [
      {
        text: "Visit python.org/downloads and download the installer for your OS",
        textVi: "Truy cập python.org/downloads và tải trình cài đặt cho hệ điều hành của bạn"
      },
      {
        text: "Run the installer. On Windows, tick \"Add Python to PATH\" before clicking Install",
        textVi: "Chạy trình cài đặt. Trên Windows, chọn \"Add Python to PATH\" trước khi nhấn Install"
      },
      {
        text: "Open a terminal and verify the installation",
        textVi: "Mở terminal và xác minh cài đặt",
        cmd: "python --version"
      },
      {
        text: "If the above doesn't work, try python3",
        textVi: "Nếu lệnh trên không hoạt động, thử python3",
        cmd: "python3 --version"
      }
    ]
  },
  {
    title: "Install VS Code + Python extension",
    titleVi: "Cài VS Code + Python extension",
    substeps: [
      {
        text: "Download and install VS Code from code.visualstudio.com",
        textVi: "Tải và cài VS Code từ code.visualstudio.com"
      },
      {
        text: "Open VS Code, then open the Extensions panel",
        textVi: "Mở VS Code, sau đó mở bảng Extensions",
        cmd: "Ctrl+Shift+X  (macOS: Cmd+Shift+X)"
      },
      {
        text: "Search for \"Python\" and install the extension by Microsoft",
        textVi: "Tìm \"Python\" và cài extension của Microsoft"
      },
      {
        text: "Open Command Palette and select a Python interpreter",
        textVi: "Mở Command Palette và chọn Python interpreter",
        cmd: "Ctrl+Shift+P → Python: Select Interpreter"
      }
    ]
  },
  {
    title: "Create qa-practice/ with a virtual environment",
    titleVi: "Tạo thư mục qa-practice/ với môi trường ảo",
    substeps: [
      {
        text: "Create the project folder and enter it",
        textVi: "Tạo thư mục dự án và vào trong",
        cmd: "mkdir qa-practice && cd qa-practice"
      },
      {
        text: "Create a virtual environment named .venv",
        textVi: "Tạo môi trường ảo tên .venv",
        cmd: "python -m venv .venv"
      },
      {
        text: "Activate it — macOS / Linux",
        textVi: "Kích hoạt — macOS / Linux",
        cmd: "source .venv/bin/activate"
      },
      {
        text: "Activate it — Windows",
        textVi: "Kích hoạt — Windows",
        cmd: ".venv\\Scripts\\activate"
      },
      {
        text: "Create verify.py, paste the demo script from above, then run it",
        textVi: "Tạo verify.py, dán script demo từ phần trên vào, sau đó chạy",
        cmd: "python verify.py"
      }
    ]
  }
]} />

## Quiz

<Quiz questions={[
  {
    q: "What is the purpose of a Python virtual environment?",
    qVi: "Mục đích của môi trường ảo Python là gì?",
    choices: [
      "To make Python run faster",
      "To isolate project dependencies so they don't conflict with other projects",
      "To enable dark mode in VS Code",
      "To connect to a remote server"
    ],
    choicesVi: [
      "Để Python chạy nhanh hơn",
      "Để cô lập các phụ thuộc của dự án để không xung đột với các dự án khác",
      "Để bật chế độ tối trong VS Code",
      "Để kết nối với máy chủ từ xa"
    ],
    answer: 1,
    explain: "Virtual environments create a project-specific sandbox for packages. Without them, installing a new version of a library for one project could break another.",
    explainVi: "Môi trường ảo tạo ra một hộp cát riêng cho các gói của dự án. Không có chúng, cài đặt phiên bản mới của thư viện cho một dự án có thể làm hỏng dự án khác."
  },
  {
    q: "Which command installs a Python package?",
    qVi: "Lệnh nào cài đặt gói Python?",
    choices: [
      "python install requests",
      "pip install requests",
      "npm install requests",
      "python -m install requests"
    ],
    choicesVi: [
      "python install requests",
      "pip install requests",
      "npm install requests",
      "python -m install requests"
    ],
    answer: 1,
    explain: "pip is Python's package installer. Always activate your virtual environment first so packages are installed into the project sandbox, not system-wide.",
    explainVi: "pip là trình cài đặt gói của Python. Luôn kích hoạt môi trường ảo của bạn trước để các gói được cài vào hộp cát của dự án, không phải toàn hệ thống."
  },
  {
    q: "Which command creates a virtual environment named 'venv'?",
    qVi: "Lệnh nào tạo môi trường ảo có tên 'venv'?",
    choices: [
      "python create venv",
      "python -m venv venv",
      "pip create venv",
      "virtualenv install venv"
    ],
    choicesVi: [
      "python create venv",
      "python -m venv venv",
      "pip create venv",
      "virtualenv install venv"
    ],
    answer: 1,
    explain: "python -m venv venv runs the built-in venv module (-m flag) and creates a virtual environment directory named 'venv'. The second 'venv' is just the chosen folder name — you can use any name.",
    explainVi: "python -m venv venv chạy module venv tích hợp (cờ -m) và tạo thư mục môi trường ảo có tên 'venv'. 'venv' thứ hai chỉ là tên thư mục được chọn — bạn có thể dùng bất kỳ tên nào."
  },
  {
    q: "On macOS/Linux, how do you activate a virtual environment located in a folder called 'venv'?",
    qVi: "Trên macOS/Linux, làm thế nào để kích hoạt môi trường ảo nằm trong thư mục 'venv'?",
    choices: [
      "venv/activate",
      "source venv/bin/activate",
      "python venv/activate",
      "pip activate venv"
    ],
    choicesVi: [
      "venv/activate",
      "source venv/bin/activate",
      "python venv/activate",
      "pip activate venv"
    ],
    answer: 1,
    explain: "On macOS/Linux use 'source venv/bin/activate'. On Windows use '.\\venv\\Scripts\\activate'. After activation, your terminal prompt changes and pip installs go into the sandbox.",
    explainVi: "Trên macOS/Linux dùng 'source venv/bin/activate'. Trên Windows dùng '.\\venv\\Scripts\\activate'. Sau khi kích hoạt, dấu nhắc terminal thay đổi và pip cài vào hộp cát."
  },
  {
    q: "What is the purpose of a `requirements.txt` file?",
    qVi: "Mục đích của file `requirements.txt` là gì?",
    choices: [
      "It documents all test cases for the project",
      "It lists the project's dependencies so anyone can install the same packages with one command",
      "It stores environment variables like API keys",
      "It configures VS Code settings and extensions"
    ],
    choicesVi: [
      "Nó ghi lại tất cả test case cho dự án",
      "Nó liệt kê các phụ thuộc của dự án để bất kỳ ai cũng có thể cài các gói giống nhau bằng một lệnh",
      "Nó lưu trữ các biến môi trường như API key",
      "Nó cấu hình cài đặt và tiện ích mở rộng VS Code"
    ],
    answer: 1,
    explain: "requirements.txt is generated with 'pip freeze > requirements.txt' and restored with 'pip install -r requirements.txt'. It ensures every team member and CI environment uses identical package versions.",
    explainVi: "requirements.txt được tạo bằng 'pip freeze > requirements.txt' và khôi phục bằng 'pip install -r requirements.txt'. Nó đảm bảo mọi thành viên nhóm và môi trường CI sử dụng cùng phiên bản gói."
  },
  {
    q: "What does running `python --version` verify?",
    qVi: "Chạy `python --version` xác minh điều gì?",
    choices: [
      "Whether your Python script has any syntax errors",
      "Whether Python is installed and shows the installed version number",
      "Whether your virtual environment is currently active",
      "Whether pip is up to date"
    ],
    choicesVi: [
      "Liệu script Python của bạn có lỗi cú pháp không",
      "Liệu Python đã được cài đặt và hiển thị số phiên bản đã cài",
      "Liệu môi trường ảo của bạn hiện đang hoạt động",
      "Liệu pip có được cập nhật không"
    ],
    answer: 1,
    explain: "python --version (or python3 --version on some systems) prints the installed Python version, confirming the interpreter is on your PATH. It's the first thing to check after installation.",
    explainVi: "python --version (hoặc python3 --version trên một số hệ thống) in phiên bản Python đã cài đặt, xác nhận trình thông dịch có trong PATH của bạn. Đây là điều đầu tiên cần kiểm tra sau khi cài đặt."
  }
]} />

## Resources

<ResourceList resources={[
  { title: "Python Downloads", url: "https://www.python.org/downloads/" },
  { title: "Python Tutorial — python.org", url: "https://docs.python.org/3/tutorial/" },
  { title: "Real Python — Python Basics", url: "https://realpython.com/" }
]} />
