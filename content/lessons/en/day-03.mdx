---
id: 3
title: "Test case design"
phase: 1
estMinutes: 75
tags: ["fundamentals", "design"]
---

## Intro

<En>
A test case is a documented set of conditions, steps, and expected results used to verify that a feature works correctly. Writing good test cases is both a science and an art — two powerful techniques guide most of the work.
</En>
<Vi>
Một test case là một tập hợp các điều kiện, bước thực hiện và kết quả mong đợi được ghi lại để xác minh rằng một tính năng hoạt động đúng. Viết test case tốt vừa là khoa học vừa là nghệ thuật — hai kỹ thuật mạnh mẽ hướng dẫn hầu hết công việc.
</Vi>

## Concept

<En>
**Equivalence Partitioning (EP)** divides input values into groups (partitions) where all values in a group behave the same way. Instead of testing every possible input, you test one representative from each partition.

Example — password length field (6–20 characters):
- Invalid partition 1: too short (1–5 chars) → test with 4
- Valid partition: acceptable length (6–20 chars) → test with 10
- Invalid partition 2: too long (>20 chars) → test with 25

By testing one value per partition you cover all classes of behaviour without redundancy.

**Boundary Value Analysis (BVA)** focuses on values at the edges of partitions, where bugs most commonly hide. For the same password field:
- Boundaries: 5 (just below), 6 (minimum valid), 20 (maximum valid), 21 (just above)

Test all four boundary values for maximum bug-catching power. EP and BVA are usually combined.
</En>
<Vi>
**Phân vùng tương đương (EP)** chia các giá trị đầu vào thành các nhóm (phân vùng) mà tất cả các giá trị trong một nhóm có hành vi giống nhau. Thay vì kiểm tra mọi đầu vào có thể, bạn kiểm tra một đại diện từ mỗi phân vùng.

Ví dụ — trường độ dài mật khẩu (6–20 ký tự):
- Phân vùng không hợp lệ 1: quá ngắn (1–5 ký tự) → kiểm tra với 4
- Phân vùng hợp lệ: độ dài chấp nhận được (6–20 ký tự) → kiểm tra với 10
- Phân vùng không hợp lệ 2: quá dài (>20 ký tự) → kiểm tra với 25

Bằng cách kiểm tra một giá trị cho mỗi phân vùng, bạn bao phủ tất cả các lớp hành vi mà không có sự dư thừa.

**Phân tích giá trị biên (BVA)** tập trung vào các giá trị tại ranh giới của các phân vùng, nơi bug thường ẩn náu nhất. Đối với trường mật khẩu tương tự:
- Các giá trị biên: 5 (dưới tối thiểu một chút), 6 (tối thiểu hợp lệ), 20 (tối đa hợp lệ), 21 (trên tối đa một chút)

Kiểm tra cả bốn giá trị biên để có sức mạnh phát hiện bug tối đa. EP và BVA thường được kết hợp với nhau.
</Vi>

## Code / Demo

```python
# Applying EP and BVA to a password validation function
def is_valid_password(password: str) -> bool:
    return 6 <= len(password) <= 20

# EP test cases
assert is_valid_password("hi") is False         # Invalid: too short (EP class 1)
assert is_valid_password("hello123") is True    # Valid (EP class 2)
assert is_valid_password("a" * 25) is False     # Invalid: too long (EP class 3)

# BVA test cases (edges of the valid partition)
assert is_valid_password("a" * 5) is False      # BVA: just below minimum
assert is_valid_password("a" * 6) is True       # BVA: exactly at minimum
assert is_valid_password("a" * 20) is True      # BVA: exactly at maximum
assert is_valid_password("a" * 21) is False     # BVA: just above maximum

print("All EP and BVA tests passed!")
```

## Exercise

<ExerciseBox>
<En>
- [ ] For an age input field (1–120), list all EP partitions and boundary values
- [ ] Write 4 test cases using EP for a quantity field that accepts 1–99
- [ ] Describe a real feature where BVA helped you catch a bug that a simple "happy path" test missed
</En>
<Vi>
- [ ] Với trường nhập tuổi (1–120), liệt kê tất cả các phân vùng EP và giá trị biên
- [ ] Viết 4 test case dùng EP cho trường số lượng chấp nhận 1–99
- [ ] Mô tả một tính năng thực tế mà BVA giúp phát hiện bug mà happy path đơn giản đã bỏ sót
</Vi>
</ExerciseBox>

## Quiz

<Quiz questions={[
  {
    q: "A text field accepts values between 10 and 100. Using EP, which values do you test?",
    qVi: "Một trường text chấp nhận giá trị từ 10 đến 100. Dùng EP, bạn kiểm thử những giá trị nào?",
    choices: [
      "Every value from 10 to 100",
      "One value below 10, one between 10–100, one above 100",
      "Only 10 and 100",
      "Only 50"
    ],
    choicesVi: [
      "Mọi giá trị từ 10 đến 100",
      "Một giá trị dưới 10, một giá trị từ 10–100, một giá trị trên 100",
      "Chỉ 10 và 100",
      "Chỉ 50"
    ],
    answer: 1,
    explain: "EP selects one representative from each partition: invalid-below, valid, and invalid-above. This covers all behaviour classes efficiently.",
    explainVi: "EP chọn một đại diện từ mỗi phân vùng: dưới-không-hợp-lệ, hợp-lệ và trên-không-hợp-lệ. Điều này bao phủ hiệu quả tất cả các lớp hành vi."
  },
  {
    q: "Why does Boundary Value Analysis focus on edges of partitions?",
    qVi: "Tại sao Phân tích Giá trị Biên tập trung vào các cạnh của phân vùng?",
    choices: [
      "Because the middle values are always correct",
      "Because bugs most commonly occur at the boundaries of valid ranges",
      "Because it is faster than testing any other value",
      "Because boundary values are required by ISTQB"
    ],
    choicesVi: [
      "Vì các giá trị ở giữa luôn đúng",
      "Vì bug thường xảy ra nhiều nhất ở các biên của phạm vi hợp lệ",
      "Vì kiểm thử nó nhanh hơn bất kỳ giá trị nào khác",
      "Vì giá trị biên được ISTQB yêu cầu"
    ],
    answer: 1,
    explain: "Off-by-one errors and fence-post bugs cluster at boundaries — a 'greater than' check written as 'greater than or equal to' only shows up when you test the exact boundary.",
    explainVi: "Lỗi off-by-one tập trung ở các biên — điều kiện 'lớn hơn' viết nhầm thành 'lớn hơn hoặc bằng' chỉ lộ ra khi bạn kiểm thử đúng biên đó."
  },
  {
    q: "A password field accepts 8–20 characters. Using BVA, which set of values is most important to test?",
    qVi: "Trường mật khẩu chấp nhận 8–20 ký tự. Dùng BVA, tập giá trị nào quan trọng nhất để kiểm thử?",
    choices: [
      "7, 8, 20, 21",
      "1, 10, 30",
      "8, 14, 20",
      "Any three values inside the valid range"
    ],
    choicesVi: [
      "7, 8, 20, 21",
      "1, 10, 30",
      "8, 14, 20",
      "Bất kỳ ba giá trị nào trong phạm vi hợp lệ"
    ],
    answer: 0,
    explain: "BVA focuses on values just outside and just inside each boundary. For 8–20: 7 (just below min), 8 (min), 20 (max), 21 (just above max) cover all four critical boundary points.",
    explainVi: "BVA tập trung vào các giá trị ngay ngoài và ngay trong mỗi biên. Cho 8–20: 7 (ngay dưới min), 8 (min), 20 (max), 21 (ngay trên max) bao phủ bốn điểm biên quan trọng."
  },
  {
    q: "When is decision table testing most useful?",
    qVi: "Khi nào kiểm thử bảng quyết định hữu ích nhất?",
    choices: [
      "When testing a single input variable in isolation",
      "When multiple input combinations produce different outputs or actions",
      "When measuring API response times",
      "When creating test data for a database"
    ],
    choicesVi: [
      "Khi kiểm thử một biến đầu vào duy nhất riêng lẻ",
      "Khi nhiều tổ hợp đầu vào tạo ra các đầu ra hoặc hành động khác nhau",
      "Khi đo thời gian phản hồi API",
      "Khi tạo dữ liệu test cho cơ sở dữ liệu"
    ],
    answer: 1,
    explain: "Decision tables systematically capture all combinations of conditions and their expected actions. They're ideal for business rules with multiple interacting inputs (e.g., discount eligibility logic).",
    explainVi: "Bảng quyết định ghi lại có hệ thống tất cả tổ hợp điều kiện và hành động mong đợi. Chúng lý tưởng cho các quy tắc kinh doanh có nhiều đầu vào tương tác (ví dụ: logic điều kiện giảm giá)."
  },
  {
    q: "What makes a test case 'good'?",
    qVi: "Điều gì tạo nên một test case 'tốt'?",
    choices: [
      "It is as long as possible to cover every scenario",
      "It is clear, repeatable, and has an unambiguous expected result",
      "It only covers the happy path",
      "It was written after the bug was found"
    ],
    choicesVi: [
      "Nó càng dài càng tốt để bao phủ mọi kịch bản",
      "Nó rõ ràng, có thể lặp lại và có kết quả mong đợi không mơ hồ",
      "Nó chỉ bao phủ happy path",
      "Nó được viết sau khi bug được tìm thấy"
    ],
    answer: 1,
    explain: "A good test case is clear (anyone can execute it), repeatable (same result every time), and has an unambiguous expected result so pass/fail is never a judgment call.",
    explainVi: "Một test case tốt là rõ ràng (ai cũng có thể thực thi), có thể lặp lại (kết quả giống nhau mỗi lần) và có kết quả mong đợi không mơ hồ để đạt/không đạt không bao giờ là phán đoán chủ quan."
  },
  {
    q: "State transition testing is most appropriate when:",
    qVi: "Kiểm thử chuyển đổi trạng thái phù hợp nhất khi:",
    choices: [
      "The system has a fixed database schema",
      "The software changes behaviour based on its current state and triggering events",
      "You need to test all boundary values",
      "The application has no UI"
    ],
    choicesVi: [
      "Hệ thống có schema cơ sở dữ liệu cố định",
      "Phần mềm thay đổi hành vi dựa trên trạng thái hiện tại và sự kiện kích hoạt",
      "Bạn cần kiểm thử tất cả giá trị biên",
      "Ứng dụng không có UI"
    ],
    answer: 1,
    explain: "State transition testing models how a system transitions between states in response to events. It's ideal for workflows, login flows, order statuses, or any feature with discrete states.",
    explainVi: "Kiểm thử chuyển đổi trạng thái mô hình hóa cách hệ thống chuyển đổi giữa các trạng thái theo sự kiện. Lý tưởng cho quy trình công việc, luồng đăng nhập, trạng thái đơn hàng hoặc bất kỳ tính năng nào có trạng thái rời rạc."
  }
]} />

## Resources

<ResourceList resources={[
  { title: "ISTQB Foundation Syllabus", url: "https://www.istqb.org/certifications/certified-tester-foundation-level" },
  { title: "Ministry of Testing — Test Design", url: "https://www.ministryoftesting.com/" }
]} />
