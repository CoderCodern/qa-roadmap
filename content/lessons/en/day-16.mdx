---
id: 16
title: "Exceptions & error handling"
phase: 2
estMinutes: 60
tags: ["python", "exceptions"]
---

## Intro

<En>
Tests will fail. Network calls will time out. Files won't exist. APIs will return unexpected data. Exception handling lets you write code that deals with these situations gracefully — either by recovering, by providing a useful error message, or by letting the exception propagate so pytest can catch and report it clearly.
</En>
<Vi>
Test sẽ thất bại. Các cuộc gọi mạng sẽ hết thời gian chờ. File sẽ không tồn tại. API sẽ trả về dữ liệu không mong đợi. Xử lý ngoại lệ cho phép bạn viết code xử lý những tình huống này một cách duyên dáng — bằng cách phục hồi, cung cấp thông báo lỗi hữu ích, hoặc để ngoại lệ lan truyền để pytest có thể bắt và báo cáo nó rõ ràng.
</Vi>

## Concept

<En>
**Basic structure:**
```python
try:
    # code that might raise an exception
except SomeError as e:
    # handle it
else:
    # runs only if no exception was raised
finally:
    # always runs — cleanup goes here
```

**Common exceptions in QA automation:**

| Exception | When it occurs |
|-----------|---------------|
| `AssertionError` | Failed `assert` statement |
| `KeyError` | Missing key in dict: `d["missing"]` |
| `IndexError` | List index out of range |
| `TypeError` | Wrong type passed to operation |
| `ValueError` | Right type but invalid value (e.g., `int("abc")`) |
| `FileNotFoundError` | File doesn't exist |
| `TimeoutError` | Operation took too long |
| `requests.HTTPError` | HTTP error response (Phase 4) |

**Raising exceptions:** Use `raise ValueError("message")` to signal that your own code received invalid input.

**Custom exceptions:** Subclass `Exception` to create domain-specific errors:
```python
class TestDataError(Exception):
    pass
```

**When NOT to catch exceptions:** Don't catch exceptions just to suppress them. Let `AssertionError` from pytest propagate — pytest knows how to format it beautifully.
</En>
<Vi>
**Cấu trúc cơ bản:**
```python
try:
    # code có thể gây ra ngoại lệ
except SomeError as e:
    # xử lý nó
else:
    # chỉ chạy nếu không có ngoại lệ
finally:
    # luôn chạy — dọn dẹp ở đây
```

**Các ngoại lệ phổ biến trong tự động hoá QA:**

| Ngoại lệ | Khi nào xảy ra |
|-----------|---------------|
| `AssertionError` | Câu lệnh `assert` thất bại |
| `KeyError` | Thiếu key trong dict |
| `IndexError` | Chỉ số danh sách ngoài phạm vi |
| `TypeError` | Sai kiểu truyền vào thao tác |
| `ValueError` | Đúng kiểu nhưng giá trị không hợp lệ |
| `FileNotFoundError` | File không tồn tại |
| `TimeoutError` | Thao tác mất quá nhiều thời gian |

**Raise ngoại lệ:** Dùng `raise ValueError("message")` để báo hiệu code của bạn nhận đầu vào không hợp lệ.

**Khi KHÔNG nên bắt ngoại lệ:** Đừng bắt ngoại lệ chỉ để chặn chúng. Hãy để `AssertionError` từ pytest lan truyền — pytest biết cách định dạng nó đẹp.
</Vi>

## Code / Demo

**Pattern 1 — `try / except`: catch a specific error**

<En>Use `except ExceptionType` to handle only the error you expect. Other errors still propagate normally.</En>
<Vi>Dùng `except ExceptionType` để chỉ xử lý lỗi bạn mong đợi. Các lỗi khác vẫn lan truyền bình thường.</Vi>

<CodePlayground code={`
def get_field(data, key, default=None):
    try:
        return data[key]
    except KeyError:        # only catches missing-key errors
        return default

response = {"status": "ok", "token": "abc123"}
token   = get_field(response, "token")
missing = get_field(response, "user_id", default=0)

assert token == "abc123"
assert missing == 0
print("Pattern 1 ok — safe dict access with fallback")
`} />

**Pattern 2 — catch then re-raise: validate input**

<En>Catch a low-level error, then raise a more descriptive one so callers get a useful message.</En>
<Vi>Bắt lỗi cấp thấp, sau đó raise lỗi mô tả hơn để caller nhận được thông báo hữu ích.</Vi>

<CodePlayground code={`
def parse_status_code(raw):
    try:
        code = int(raw)
    except ValueError:
        raise ValueError(f"'{raw}' is not a valid status code")
    if not 100 <= code <= 599:
        raise ValueError(f"Status code {code} is out of range")
    return code

assert parse_status_code("200") == 200

try:
    parse_status_code("abc")
except ValueError as e:
    print(f"Caught: {e}")

print("Pattern 2 ok — validate and re-raise")
`} />

**Pattern 3 — `finally`: code that always runs**

<En>`finally` executes whether or not an exception occurred — perfect for cleanup (closing files, releasing connections).</En>
<Vi>`finally` luôn thực thi dù có ngoại lệ hay không — hoàn hảo cho việc dọn dẹp (đóng file, giải phóng kết nối).</Vi>

<CodePlayground code={`
opened = False
closed = False

def use_resource():
    global opened, closed
    opened = True
    try:
        raise RuntimeError("something went wrong")
    finally:
        closed = True   # runs even though an exception was raised

try:
    use_resource()
except RuntimeError:
    pass

assert opened and closed
print("Pattern 3 ok — finally always ran")
`} />

**Pattern 4 — custom exception class**

<En>Subclass `Exception` to create your own error type. This lets callers catch your specific error without catching everything.</En>
<Vi>Kế thừa `Exception` để tạo kiểu lỗi riêng. Caller có thể bắt đúng lỗi của bạn mà không cần bắt tất cả.</Vi>

<CodePlayground code={`
class InvalidTestDataError(Exception):
    pass

def create_user(email, role):
    valid_roles = {"admin", "editor", "viewer"}
    if role not in valid_roles:
        raise InvalidTestDataError(f"Invalid role '{role}'")
    return {"email": email, "role": role}

try:
    create_user("alice@example.com", "superuser")
except InvalidTestDataError as e:
    print(f"Caught custom error: {e}")

user = create_user("bob@example.com", "admin")
assert user["role"] == "admin"
print("Pattern 4 ok — custom exception works")
`} />

## Exercise

<CodeExerciseGroup pointsPerExercise={15} exercises={[
  {
    title: "Fill in ??? with the exception raised when a dict key is missing.",
    code: `def get_status(data):
    try:
        return data["status"]
    except ???:  # TODO (EN): which exception for a missing dict key?
                  # TODO (VI): ngoại lệ nào khi thiếu key trong dict?
        return "unknown"

r1 = {"status": "ok"}
r2 = {"code": 200}
assert get_status(r1) == "ok"
assert get_status(r2) == "unknown"
print("Safe access works!")`,
  },
  {
    title: "Fill in ??? with the correct built-in exception to raise for an invalid value.",
    code: `def validate_age(age):
    if age < 0:
        # TODO (EN): raise the right built-in exception for an invalid value
        # TODO (VI): raise ngoại lệ tích hợp phù hợp cho giá trị không hợp lệ
        raise ???("Age cannot be negative")
    return age

try:
    validate_age(-1)
    print("ERROR: should have raised")
except ValueError as e:
    print(f"Caught: {e}")

assert validate_age(25) == 25
print("Validation works!")`,
  },
  {
    title: "Fill in ??? with the class that every custom exception must inherit from.",
    code: `# TODO (EN): custom exceptions must inherit from this class
# TODO (VI): custom exception phải kế thừa từ class này
class ApiError(???):
    pass

closed = False

def call_api(status):
    global closed
    try:
        if status != 200:
            raise ApiError(f"Bad status: {status}")
        return "ok"
    finally:
        closed = True  # always runs

try:
    call_api(404)
except ApiError as e:
    print(f"Caught: {e}")

assert closed, "finally must have run"
print("Custom exception and finally work!")`,
  },
]} />

## Quiz

<Quiz questions={[
  {
    q: "When does the `finally` block execute?",
    choices: [
      "Only when an exception is raised",
      "Only when no exception is raised",
      "Always — whether or not an exception was raised",
      "Only when using a custom exception class"
    ],
    answer: 2,
    explain: "finally always runs. It's designed for cleanup code — closing files, releasing connections, resetting state — that must happen regardless of success or failure."
  },
  {
    q: "Why should you NOT catch exceptions silently (bare `except: pass`)?",
    choices: [
      "Because Python 3 removed bare except",
      "Because it hides bugs — you'll never know something went wrong",
      "Because it causes memory leaks",
      "Because it is slower than specific except clauses"
    ],
    answer: 1,
    explain: "Silently swallowing exceptions is one of the worst coding habits. Tests that never fail even when things go wrong give false confidence. Always either handle the exception meaningfully or let it propagate."
  },
  {
    q: "Which exception is raised when you access a dict key that does not exist?",
    choices: [
      "IndexError",
      "ValueError",
      "KeyError",
      "AttributeError"
    ],
    answer: 2,
    explain: "KeyError is raised when a dict key is not found. IndexError is for lists/tuples when an index is out of range. Use dict.get(key, default) or a try/except KeyError to handle missing keys safely."
  },
  {
    q: "What is the correct way to create a custom exception class in Python?",
    choices: [
      "class MyError: pass",
      "class MyError(Exception): pass",
      "class MyError(Error): pass",
      "def MyError(Exception): pass"
    ],
    answer: 1,
    explain: "Custom exceptions must subclass Exception (or one of its subclasses). This makes them catchable by standard except clauses and gives them the full exception interface (__str__, args, etc.)."
  },
  {
    q: "What does the `else` clause in a try/except block do?",
    choices: [
      "It runs only when an exception was raised",
      "It runs only when NO exception was raised in the try block",
      "It is equivalent to finally",
      "It catches all remaining exception types"
    ],
    answer: 1,
    explain: "The else clause runs when the try block completes without raising any exception. It lets you put success-path code where it's clear it only runs on success, keeping it separate from error-handling code."
  },
  {
    q: "What happens if an exception is raised inside a `finally` block?",
    choices: [
      "Python ignores it and continues normally",
      "It is silently swallowed",
      "It replaces the original exception and propagates",
      "Python runs the except block again"
    ],
    answer: 2,
    explain: "If finally raises a new exception, the original exception is discarded and the new one propagates instead. This is a common bug — keep finally blocks simple and exception-free."
  },
  {
    q: "Which of these is the safest way to access a potentially missing key in a dict?",
    choices: [
      "data[key]",
      "data.get(key, default)",
      "try: data[key] except Exception: pass",
      "data.find(key)"
    ],
    answer: 1,
    explain: "dict.get(key, default) returns the default if the key is absent, with no exception. It's concise, readable, and idiomatic Python. Bare except: pass is dangerous because it hides all errors."
  },
  {
    q: "In a try/except/else/finally block, what is the execution order when NO exception is raised?",
    choices: [
      "try → except → finally",
      "try → finally → else",
      "try → else → finally",
      "try → else → except → finally"
    ],
    answer: 2,
    explain: "When no exception occurs: try runs, then else runs (success path), then finally runs (always). except is skipped entirely. This order lets each clause have a single clear responsibility."
  },
  {
    q: "Which exception should you raise when a function receives an argument of the right type but an invalid value?",
    choices: [
      "TypeError",
      "RuntimeError",
      "ValueError",
      "AssertionError"
    ],
    answer: 2,
    explain: "ValueError signals that an argument has the correct type but an unacceptable value (e.g., int('-1') for an age field). TypeError is for wrong types. AssertionError is for internal invariant checks, not public API validation."
  },
  {
    q: "What is the main advantage of raising a specific custom exception (e.g., `ApiError`) over a generic `Exception`?",
    choices: [
      "Custom exceptions run faster",
      "Callers can catch only your specific error without accidentally catching unrelated exceptions",
      "Python requires custom exceptions for all raise statements",
      "Custom exceptions automatically log themselves"
    ],
    answer: 1,
    explain: "With a custom exception, callers write `except ApiError` and only handle your error. If you raise bare Exception, callers who write `except Exception` will accidentally swallow all other bugs too, making debugging very hard."
  }
]} />

## Resources

<ResourceList resources={[
  { title: "Python Exceptions — docs.python.org", url: "https://docs.python.org/3/tutorial/errors.html" },
  { title: "Real Python — Exceptions", url: "https://realpython.com/" }
]} />
